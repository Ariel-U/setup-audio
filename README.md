# setup-audio
Un script en bash para automatizar la preparación del sistema para trabajar con audio.

Requiere permisos elevados ya que modifica los archivos del sistema `/etc/security/limits.conf` y`/etc/sysctl.conf`
